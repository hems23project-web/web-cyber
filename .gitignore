node_modules
dist
dist-ssr
.vite
*.local
.DS_Store
coverage
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# NOTE: public/photos/ is intentionally NOT ignored —
# drop your own photos in there and commit them so they deploy with the site.
# See public/photos/README.md for the exact filenames.

# verification harness output
.verify

# tsc incremental build info
*.tsbuildinfo

# THE SOLVE GUIDE. Full spoilers for the entire game. Gitignored so it can
# never be committed, deployed, or linked by accident.
WALKTHROUGH.md

# deployment packaging scratch space
.deploy

# mi-birthday-site.zip IS committed on purpose: it is the ready-to-drop build
# handed to whoever deploys this, and they should not need a toolchain to get it.
# Regenerate with:  npm run build && ./scripts/package.sh

# Standalone QR artifacts for printing/stitching. Delivered directly,
# never part of the site or the repo.
qr/
