# public/assets

Anything static that isn't a photo: a custom favicon set, an og-image,
a printable PDF of the timeline, a font file you'd rather self-host.

Reference it from the app with a root-absolute path, e.g. `/assets/og.png`.
